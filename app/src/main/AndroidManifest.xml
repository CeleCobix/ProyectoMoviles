<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComercializadoraNorte">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.RecuperarAccesoActivity"
            android:exported="false"
            android:label="@string/titulo_recuperar_acceso" />

        <activity
            android:name=".ui.login.NuevaContrasenaActivity"
            android:exported="false"
            android:label="@string/titulo_nueva_contrasena" />

        <activity
            android:name=".ui.reportes.RankingClientesActivity"
            android:exported="false"
            android:label="@string/ranking_clientes" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>